name: Build and Deploy to Azure Container App

on:
  push:
    branches:
      - main

env:
  ACR_NAME: acrjovanlab001
  IMAGE_NAME: demo-app

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - uses: azure/login@v2
        with:
          creds: ${{ secrets.AZURE_CREDENTIALS }}

      - run: az acr login --name $ACR_NAME

      - run: |
          docker build -f ./app/Dockerfile -t $ACR_NAME.azurecr.io/$IMAGE_NAME:${{ github.sha }} ./app
          docker push $ACR_NAME.azurecr.io/$IMAGE_NAME:${{ github.sha }}

      - run: |
          az extension add --name containerapp --upgrade

          echo "=== ACCOUNT ==="
          az account show

          echo "=== RESOURCE GROUPS ==="
          az group list -o table

          echo "=== CONTAINER APPS IN RG ==="
          az containerapp list --resource-group rg-containerapp-lab -o table

          echo "=== SHOW EXACT APP ==="
          az containerapp show \
            --name demo-app-container \
            --resource-group rg-containerapp-lab

          echo "=== TRY UPDATE ==="
          az containerapp update \
            --name demo-app-container \
            --resource-group rg-containerapp-lab \
            --image $ACR_NAME.azurecr.io/$IMAGE_NAME:${{ github.sha }}
